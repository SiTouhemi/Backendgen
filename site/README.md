# BackendGen launch page

This is a dependency-free static page. Preview it from the repository root:

```sh
npx --yes serve site
```

The page deliberately does not include analytics, a signup form, invented
testimonials, or a token-savings claim. Add a real repository URL and contact
channel only after the public owner is final.
